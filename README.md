# 🎬 YouTube Downloader Bot (Pyrogram)

Telegram бот для скачивания видео с YouTube с поддержкой файлов **до 2 ГБ**.

## ✨ Возможности

- 📹 Скачивание видео в MP4
- 🎵 Извлечение аудио в MP3
- 📊 Отображение прогресса загрузки
- 🚀 Быстрая передача через MTProto
- 📦 Поддержка файлов до 2 ГБ (вместо 50 МБ в Bot API)

## 🛠 Установка

### 1. Клонируй/скачай файлы

```bash
# Создай папку проекта
mkdir youtube-bot && cd youtube-bot
```

### 2. Установи зависимости

```bash
pip install -r requirements.txt
```

Или вручную:
```bash
pip install pyrogram tgcrypto yt-dlp
```

### 3. Установи FFmpeg (для извлечения аудио)

**Windows:**
```bash
# Через winget
winget install FFmpeg

# Или через chocolatey
choco install ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt update && sudo apt install ffmpeg
```

## ⚙️ Настройка

### Шаг 1: Получи API_ID и API_HASH

1. Перейди на [my.telegram.org](https://my.telegram.org)
2. Войди по номеру телефона
3. Нажми **"API development tools"**
4. Создай приложение (название любое)
5. Скопируй `api_id` и `api_hash`

### Шаг 2: Получи токен бота

1. Открой [@BotFather](https://t.me/BotFather) в Telegram
2. Отправь `/newbot`
3. Придумай имя и username
4. Скопируй токен

### Шаг 3: Заполни настройки в боте

Открой `youtube_bot_pyrogram.py` и замени:

```python
API_ID = 12345678  # Твой API_ID (число)
API_HASH = "your_api_hash_here"  # Твой API_HASH (строка)
BOT_TOKEN = "your_bot_token_here"  # Токен от BotFather
```

## 🚀 Запуск

```bash
python youtube_bot_pyrogram.py
```

При первом запуске Pyrogram создаст файл сессии `youtube_downloader_bot.session`.

## 📝 Использование

1. Найди своего бота в Telegram
2. Отправь `/start`
3. Отправь ссылку на YouTube видео
4. Выбери формат (видео или аудио)
5. Дождись загрузки

## ⚠️ Ограничения

| Параметр | Лимит |
|----------|-------|
| Размер файла | 2 ГБ |
| Источник | Только YouTube |

## 🔧 Решение проблем

### "Sign in required" / Ошибки авторизации
- Проверь правильность API_ID, API_HASH и BOT_TOKEN
- Убедись, что токен бота актуален

### "FFmpeg not found"
- Установи FFmpeg (см. инструкцию выше)
- Перезапусти терминал после установки

### Видео не скачивается
- Проверь, что видео не приватное
- Попробуй обновить yt-dlp: `pip install -U yt-dlp`
- YouTube может временно блокировать — подожди и попробуй снова

### Медленная загрузка
- Это зависит от скорости интернета и серверов YouTube
- Для больших видео загрузка может занять несколько минут

## 📁 Структура проекта

```
youtube-bot/
├── youtube_bot_pyrogram.py  # Основной код бота
├── requirements.txt         # Зависимости
├── downloads/              # Временные файлы (создаётся автоматически)
└── youtube_downloader_bot.session  # Сессия Pyrogram (создаётся при запуске)
```

## 🔄 Сравнение с Bot API

| Параметр | Bot API | Pyrogram (MTProto) |
|----------|---------|-------------------|
| Лимит файла | 50 МБ | **2 ГБ** |
| Скорость | Обычная | **Быстрая** |
| Прогресс | Нет | **Есть** |

## 📜 Лицензия

MIT — используй как хочешь.
